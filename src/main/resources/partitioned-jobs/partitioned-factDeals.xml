<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:batch="http://www.springframework.org/schema/batch"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch.xsd">

    <import resource="../simple-jobs/simple-factDeals.xml"/>

    <bean id="partitionerFactDealsByLines" class="partitioner.PartitionerByLines">
        <property name="batchSize" value="1000000"/>
        <property name="pathToFile" value="/home-srv2/projects/SPBBank/input_data/raw_data/step_1/FactDeals.csv"/>
        <property name="tempDir" value="/home-srv2/dbrosalin/temp_dir"/>
    </bean>

    <bean id="taskExecutorFactDeals"
          class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor"
          destroy-method="shutdown">
        <property name="maxPoolSize" value="12"/>
        <property name="corePoolSize" value="10"/>
        <property name="queueCapacity" value="13"/>
    </bean>

    <bean id="executorFactDeals" class="org.springframework.core.task.SimpleAsyncTaskExecutor">
    </bean>

    <batch:job id="masterFactDealsJob" job-repository="jobRepository">
        <batch:step id="masterFactDealsStep">
            <batch:partition step="slaveFactDealsStep" partitioner="partitionerFactDealsByLines">
                <batch:handler task-executor="executorFactDeals" grid-size="10"/>
            </batch:partition>
        </batch:step>
    </batch:job>

    <batch:step id="slaveFactDealsStep">
        <batch:tasklet throttle-limit="10">
            <batch:chunk reader="batchFactDealsCsv"
                         writer="factDealsWriter"
                         commit-interval="150">
                <batch:skip-policy>
                    <bean class="org.springframework.batch.core.step.skip.AlwaysSkipItemSkipPolicy"/>
                </batch:skip-policy>
            </batch:chunk>
        </batch:tasklet>
    </batch:step>

</beans>